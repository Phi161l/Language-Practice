# this Keyword Practice

Experiments to understand:

- How `this` behaves in different contexts
- Method calls vs regular function calls
- call / apply / bind
- Arrow function `this` behavior

Explanations are written inside the code.
